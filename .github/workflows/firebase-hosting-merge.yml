# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "Create env file"
        run: |
          touch .env
          echo -e REACT_APP_MAPBOX_GL_KEY=${{ secrets.REACT_APP_MAPBOX_GL_KEY }} > .env
          echo -e REACT_APP_FIREBASE_KEY=${{ secrets.REACT_APP_FIREBASE_KEY }} >> .env
          cat .env
        working-directory: client
      - run: npm ci && npm run build
        working-directory: client
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: ./client
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZEPHYR_3FB26 }}"
          channelId: live
          projectId: zephyr-3fb26
